node_modules/
dist/
*.log
.DS_Store
# Blender render scratch (per-frame cells before they're packed into the sheets)
public/assets/sprites/units/_tmp_frames/
tools/blender/*.blend
tools/blender/*.blend1
# Raw 3D inputs (Mixamo/Meshy FBX/GLB) are render INPUTS only — never shipped; the rendered PNG sheets are.
assets/raw/
*.fbx
*.glb
__pycache__/
*.pyc

# Meshy integration (tools/meshy) — never commit the real key; DO ship the template.
tools/meshy/.env
!tools/meshy/.env.example
# Pulled Meshy assets (assets/raw/meshy/) are already covered by `assets/raw/` above.
